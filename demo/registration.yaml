apiVersion: elemental.cattle.io/v1beta1
kind: MachineRegistration
metadata:
  name: walotectnode3
  namespace: fleet-default
spec:
  config:
    cloud-config:
      users:
        - name: root
          passwd: root
    elemental:
      install:
        reboot: true
        device: /nvme0n1/nvme0n1p3
        debug: true
  machineName: walotectmachine3
  machineInventoryLabels:
    location: "europewalotect13"
